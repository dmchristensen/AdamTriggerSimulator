<Window x:Class="AdamTriggerSimulator.Views.ProfileEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AdamTriggerSimulator.ViewModels"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:ProfileEditorDialogViewModel}"
        Title="{Binding DialogTitle}"
        Width="450" Height="520"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        KeyDown="Window_KeyDown">

    <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Background="{DynamicResource ApplicationBackgroundBrush}">
        <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Dialog Title -->
        <TextBlock Grid.Row="0"
                   Text="{Binding DialogTitle}"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,20"/>

        <!-- Form Fields -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Profile Name:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ui:TextBox Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Enter profile name"
                        Margin="0,0,0,20"/>

            <TextBlock Text="IP Address:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ui:TextBox Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="192.168.0.31"
                        Margin="0,0,0,20"/>

            <TextBlock Text="Port:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ui:NumberBox Value="{Binding Port}"
                          Minimum="1"
                          Maximum="65535"
                          SpinButtonPlacementMode="Compact"
                          Margin="0,0,0,20"/>

            <TextBlock Text="Description (Optional):" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ui:TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Enter description"
                        AcceptsReturn="True"
                        Height="80"
                        TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <ui:Button Content="Cancel"
                       Command="{Binding CancelCommand}"
                       Click="CancelButton_Click"
                       Margin="0,0,10,0"
                       Padding="20,8"
                       MinWidth="100"/>
            <ui:Button Content="Save"
                       Command="{Binding SaveCommand}"
                       Click="SaveButton_Click"
                       Appearance="Primary"
                       Padding="20,8"
                       MinWidth="100"/>
        </StackPanel>
        </Grid>
    </Border>
</Window>
